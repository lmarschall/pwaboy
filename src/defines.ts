export enum EControl {
  UP,
  RIGHT,
  DOWN,
  LEFT,
  A,
  B,
  SELECT,
  START,
}

export enum EButton {
  LOAD,
  CONTINUE,
  NEW_GAME,
  GO_BACK,
  IMPORT,
  EXPORT,
}

export enum EInterfaceStates {
  LOAD_ROM,
  START_GAME,
  PLAY,
  TRANSITION,
}
